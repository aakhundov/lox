Checks: >
  bugprone-*,
  cert-*,
  clang-analyzer-*,
  misc-*,
  performance-*,
  portability-*,
  readability-*,
  -bugprone-easily-swappable-parameters,
  -readability-identifier-length,
  -readability-implicit-bool-conversion,
  -misc-no-recursion,
  -clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling

# our own headers only: vendored third-party code is not ours to lint
HeaderFilterRegex: '.*/clox/src/.*'
FormatStyle: file

CheckOptions:
  # fileno() is declared in a private Darwin header pulled in by <stdio.h>
  misc-include-cleaner.IgnoreHeaders: '.*/_stdio\.h'
